# Copyright 2023 Google LLC
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#      http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.0
  labels:
    configmanagement.gke.io/arch: csmr
    configmanagement.gke.io/system: "true"
  name: notifications.configsync.gke.io
spec:
  group: configsync.gke.io
  names:
    kind: Notification
    listKind: NotificationList
    plural: notifications
    singular: notification
  preserveUnknownFields: false
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.commit
      name: Commit
      type: string
    - jsonPath: .status.LastUpdate
      name: LastUpdate
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Notification represents the notification configuration for a
          RootSync or RepoSync object. It is created and managed by Config Sync.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          status:
            description: NotificationStatus represents the status of the notification
              controller for a RootSync or RepoSync with notifications enabled.
            properties:
              commit:
                description: hash of the source of truth that is rendered. It can
                  be a git commit hash, or an OCI image digest.
                type: string
              deliveries:
                description: deliveries is a list of notifications which were delivered
                  for the change indicated by commit.
                items:
                  description: NotificationDelivery represents a notification that
                    was successfully delivered
                  properties:
                    alreadyNotified:
                      description: alreadyNotified indicates that this notification
                        was already delivered in a previous iteration
                      type: boolean
                    recipient:
                      description: recipient is the recipient that the notification
                        delivery is sent to. For example, this could be an email address
                        for a notification with service of type email.
                      type: string
                    service:
                      description: service is the service that the notification delivery
                        is sent to. For example github, slack, etc.
                      type: string
                    trigger:
                      description: trigger is the name of the trigger which caused
                        this notification delivery to be sent.
                      type: string
                  type: object
                type: array
              errors:
                description: errors is a list of any errors that occurred while sending
                  notifications from the change indicated by commit.
                items:
                  description: NotificationError represents an error which occurred
                    within the notification controller for a RootSync or RepoSync.
                  properties:
                    errorMessage:
                      description: errorMessage describes the error that occurred.
                      type: string
                  type: object
                type: array
              lastUpdate:
                description: lastUpdate is the timestamp of when this status was last
                  updated by a reconciler.
                format: date-time
                type: string
              observedGeneration:
                description: observedGeneration is the most recent generation observed
                  for the RootSync/RepoSync resource. It corresponds to the RootSync/RepoSync
                  generation, which is updated on mutation by the API Server.
                format: int64
                type: integer
              warnings:
                description: warnings is a list of any warnings that occurred while
                  sending notifications from the change indicated by commit.
                items:
                  description: NotificationError represents an error which occurred
                    within the notification controller for a RootSync or RepoSync.
                  properties:
                    errorMessage:
                      description: errorMessage describes the error that occurred.
                      type: string
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: false
    subresources:
      status: {}
  - additionalPrinterColumns:
    - jsonPath: .status.commit
      name: Commit
      type: string
    - jsonPath: .status.LastUpdate
      name: LastUpdate
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: Notification represents the notification configuration for a
          RootSync or RepoSync object. It is created and managed by Config Sync.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          status:
            description: NotificationStatus represents the status of the notification
              controller for a RootSync or RepoSync with notifications enabled.
            properties:
              commit:
                description: hash of the source of truth that is rendered. It can
                  be a git commit hash, or an OCI image digest.
                type: string
              deliveries:
                description: deliveries is a list of notifications which were delivered
                  for the change indicated by commit.
                items:
                  description: NotificationDelivery represents a notification that
                    was successfully delivered
                  properties:
                    alreadyNotified:
                      description: alreadyNotified indicates that this notification
                        was already delivered in a previous iteration
                      type: boolean
                    recipient:
                      description: recipient is the recipient that the notification
                        delivery is sent to. For example, this could be an email address
                        for a notification with service of type email.
                      type: string
                    service:
                      description: service is the service that the notification delivery
                        is sent to. For example github, slack, etc.
                      type: string
                    trigger:
                      description: trigger is the name of the trigger which caused
                        this notification delivery to be sent.
                      type: string
                  type: object
                type: array
              errors:
                description: errors is a list of any errors that occurred while sending
                  notifications from the change indicated by commit.
                items:
                  description: NotificationError represents an error which occurred
                    within the notification controller for a RootSync or RepoSync.
                  properties:
                    errorMessage:
                      description: errorMessage describes the error that occurred.
                      type: string
                  type: object
                type: array
              lastUpdate:
                description: lastUpdate is the timestamp of when this status was last
                  updated by a reconciler.
                format: date-time
                type: string
              observedGeneration:
                description: observedGeneration is the most recent generation observed
                  for the RootSync/RepoSync resource. It corresponds to the RootSync/RepoSync
                  generation, which is updated on mutation by the API Server.
                format: int64
                type: integer
              warnings:
                description: warnings is a list of any warnings that occurred while
                  sending notifications from the change indicated by commit.
                items:
                  description: NotificationError represents an error which occurred
                    within the notification controller for a RootSync or RepoSync.
                  properties:
                    errorMessage:
                      description: errorMessage describes the error that occurred.
                      type: string
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
